---
interface Props {
  filename?: string
  lang?: string
}

const { filename, lang } = Astro.props
---

<div class="group relative overflow-hidden rounded-xl border border-border bg-[#0a0a0f]">
  {filename && (
    <div class="flex items-center border-b border-border bg-surface/50 px-4 py-2">
      <div class="flex items-center gap-1.5 mr-3">
        <span class="h-2.5 w-2.5 rounded-full bg-[#ff5f57]"></span>
        <span class="h-2.5 w-2.5 rounded-full bg-[#febc2e]"></span>
        <span class="h-2.5 w-2.5 rounded-full bg-[#28c840]"></span>
      </div>
      <span class="text-xs font-mono text-gray-500">{filename}</span>
    </div>
  )}
  <div class="relative">
    <pre class="overflow-x-auto p-5 font-mono text-sm leading-relaxed text-gray-300"><code><slot /></code></pre>
    <button
      class="copy-btn absolute right-3 top-3 rounded-md border border-border bg-surface px-2 py-1 text-xs text-gray-500 opacity-0 transition-opacity hover:text-gray-300 group-hover:opacity-100"
      aria-label="Copy code"
    >
      Copy
    </button>
  </div>
</div>
