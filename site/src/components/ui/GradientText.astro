---
interface Props {
  as?: 'span' | 'h1' | 'h2' | 'h3'
  gradient?: string
  class?: string
}

const { as: Tag = 'span', gradient = 'from-gray-900 via-gray-600 to-indigo-600 dark:from-white dark:via-gray-200 dark:to-indigo-400', class: className = '' } = Astro.props
---

<Tag class={`bg-gradient-to-br ${gradient} bg-clip-text text-transparent ${className}`}>
  <slot />
</Tag>
