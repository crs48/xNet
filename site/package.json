{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build:llms": "tsx scripts/build-llms-full.ts",
    "validate:compare": "tsx scripts/validate-compare.ts",
    "validate:changelog": "tsx scripts/validate-changelog.ts",
    "validate:plugins": "tsx scripts/validate-plugins.ts",
    "build": "pnpm validate:compare && pnpm validate:changelog && pnpm validate:plugins && pnpm build:llms && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.37.6",
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.16.15",
    "tailwindcss": "^3.4.14"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  },
  "private": true
}
