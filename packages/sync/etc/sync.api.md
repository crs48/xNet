## API Report File for "@xnetjs/sync"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { AuthDecision } from '@xnetjs/core';
import { ContentId } from '@xnetjs/core';
import { DID } from '@xnetjs/core';
import { EncryptedData } from '@xnetjs/crypto';
import { HybridKeyBundle } from '@xnetjs/identity';
import { PolicyEvaluator } from '@xnetjs/core';
import { PQKeyRegistry } from '@xnetjs/identity';
import { SecurityLevel } from '@xnetjs/crypto';
import { SignatureWire } from '@xnetjs/crypto';
import { UnifiedSignature } from '@xnetjs/crypto';
import { WrappedKey } from '@xnetjs/crypto';

// @public
export function addDependencies(features: readonly FeatureFlag[]): FeatureFlag[];

// @public
export const ALL_FEATURES: FeatureFlag[];

// Warning: (ae-forgotten-export) The symbol "RepairOptions" needs to be exported by the entry point index.d.ts
//
// @public
export function attemptRepair(changes: Change<unknown>[], issues: IntegrityIssue[], options?: RepairOptions): Promise<{
    repaired: Change<unknown>[];
    remainingIssues: IntegrityIssue[];
    repairCount: number;
}>;

// @public
export interface AttestationVerificationResult {
    // (undocumented)
    errors: string[];
    // (undocumented)
    expired: boolean;
    // (undocumented)
    level: SecurityLevel;
    // (undocumented)
    valid: boolean;
}

// @public
export interface AttestationVerifyResult {
    // (undocumented)
    reason?: 'expired' | 'invalid_signature' | 'did_resolution_failed';
    // (undocumented)
    valid: boolean;
}

// @public (undocumented)
export type AuthorizedDoc<TDoc extends YDocLike = YDocLike> = {
    doc: TDoc;
    nodeId: string;
    mode: 'read' | 'write';
    contentKey: Uint8Array;
    room?: AuthorizedRoom<TDoc>;
    release: () => void;
};

// @public (undocumented)
export type AuthorizedRoom<TDoc extends YDocLike = YDocLike> = {
    nodeId: string;
    doc: TDoc;
    contentKey: Uint8Array;
    authGate: YjsAuthGate;
    authorizedPeers: Set<DID>;
};

// @public (undocumented)
export interface AuthorizedStateAdapter {
    // (undocumented)
    getDocumentContent(nodeId: string): Promise<Uint8Array | null>;
    // (undocumented)
    setDocumentContent(nodeId: string, content: Uint8Array): Promise<void>;
}

// @public (undocumented)
export class AuthorizedSyncManager<TDoc extends YDocLike = YDocLike> {
    constructor(options: AuthorizedSyncManagerOptions<TDoc>);
    // (undocumented)
    acquire(nodeId: string, mode?: 'read' | 'write'): Promise<AuthorizedDoc<TDoc>>;
    // (undocumented)
    release(nodeId: string): void;
}

// @public (undocumented)
export interface AuthorizedSyncManagerOptions<TDoc extends YDocLike = YDocLike> {
    // (undocumented)
    adapter: AuthorizedStateAdapter;
    // (undocumented)
    authorDID: DID;
    // (undocumented)
    evaluator: PolicyEvaluator;
    // (undocumented)
    keyProvider: ContentKeyProvider;
    // (undocumented)
    onRotateContentKey?: (input: {
        nodeId: string;
        recipients: DID[];
        wrappedKeys: Record<string, WrappedKey>;
        contentKey: Uint8Array;
    }) => Promise<void>;
    // (undocumented)
    publicKeyResolver?: RecipientKeyResolver;
    // (undocumented)
    store: GrantEventStore;
    // (undocumented)
    ydoc: YDocCodec<TDoc>;
}

// @public (undocumented)
export class AuthorizedYjsError extends Error {
    constructor(code: 'PERMISSION_DENIED', decision: AuthDecision);
    // (undocumented)
    readonly code: 'PERMISSION_DENIED';
    // (undocumented)
    readonly decision: AuthDecision;
}

// @public (undocumented)
export class AuthorizedYjsSyncProvider<TDoc extends YDocLike = YDocLike> {
    constructor(options: AuthorizedYjsSyncProviderOptions<TDoc>);
    handleRemoteUpdate(envelope: SignedYjsEnvelopeV2): Promise<boolean>;
}

// @public (undocumented)
export interface AuthorizedYjsSyncProviderOptions<TDoc extends YDocLike = YDocLike> {
    // (undocumented)
    authGate: YjsAuthGate;
    // (undocumented)
    doc: TDoc;
    // (undocumented)
    nodeId: string;
    // (undocumented)
    onRejected?: (input: {
        peerId: DID;
        reason: 'rate-exceeded' | 'oversized' | 'invalid-signature' | 'unauthorized';
    }) => void;
    // (undocumented)
    peerScorer?: YjsPeerScorer;
    // (undocumented)
    rateLimiter?: {
        allow(peerId: DID): boolean;
    };
    // (undocumented)
    verifyEnvelope?: (envelope: SignedYjsEnvelopeV2) => Promise<EnvelopeVerificationResult>;
    // (undocumented)
    ydoc: Pick<YDocCodec<TDoc>, 'applyUpdate'>;
}

// @public
export function autoDeserialize<T = unknown>(data: SerializedChange): DeserializeOutcome<T>;

// @public
export function autoSerialize<T>(change: Change<T>): SerializedChange;

// @public
export abstract class BaseSyncProvider<T = unknown> implements SyncProvider<T> {
    constructor(options: SyncProviderOptions);
    // (undocumented)
    protected addPeer(id: string, name?: string): void;
    // (undocumented)
    abstract broadcast(change: Change<T>): Promise<void>;
    canUseFeature(peerId: string, feature: FeatureFlag): boolean;
    canUseFeatureWithAll(feature: FeatureFlag): boolean;
    // (undocumented)
    abstract connect(): Promise<void>;
    // (undocumented)
    abstract disconnect(): Promise<void>;
    // (undocumented)
    protected emit<E extends keyof SyncProviderEvents<T>>(event: E, ...args: Parameters<SyncProviderEvents<T>[E]>): void;
    getCommonFeatures(): FeatureFlag[];
    getNegotiatedSession(peerId: string): NegotiatedSession | undefined;
    // (undocumented)
    protected _listeners: Map<string, Set<(...args: unknown[]) => unknown>>;
    // (undocumented)
    get localCapabilities(): PeerCapabilities;
    protected _localCapabilities: PeerCapabilities;
    protected negotiateWithPeer(peerId: string, remoteCapabilities: PeerCapabilities): NegotiationResult;
    protected _negotiator: VersionNegotiator;
    // (undocumented)
    off<E extends keyof SyncProviderEvents<T>>(event: E, listener: SyncProviderEvents<T>[E]): void;
    // (undocumented)
    on<E extends keyof SyncProviderEvents<T>>(event: E, listener: SyncProviderEvents<T>[E]): void;
    // (undocumented)
    once<E extends keyof SyncProviderEvents<T>>(event: E, listener: SyncProviderEvents<T>[E]): void;
    protected _options: SyncProviderOptions;
    // (undocumented)
    get peerInfo(): Map<string, PeerInfo>;
    // (undocumented)
    get peers(): string[];
    // (undocumented)
    protected _peers: Map<string, PeerInfo>;
    // (undocumented)
    protected removePeer(id: string): void;
    // (undocumented)
    abstract requestChanges(peerId: string, since?: string): Promise<Change<T>[]>;
    // (undocumented)
    requestChangesFromAll(since?: string): Promise<Change<T>[]>;
    // (undocumented)
    protected setStatus(status: SyncStatus): void;
    // (undocumented)
    get status(): SyncStatus;
    // (undocumented)
    protected _status: SyncStatus;
    // (undocumented)
    protected updatePeerLastSeen(id: string): void;
}

// @public
export type BatchFlushCallback = (mergedUpdate: Uint8Array, updateCount: number) => void;

// @public
export function calculateChunkCount(totalSize: number, chunkSize?: number): number;

// @public
export interface ChainValidationResult {
    error?: string;
    forkDetected?: boolean;
    forkPoint?: ContentId;
    valid: boolean;
}

// @public
export interface Change<T = unknown> {
    authorDID: DID;
    batchId?: string;
    batchIndex?: number;
    batchSize?: number;
    hash: ContentId;
    id: string;
    lamport: number;
    parentHash: ContentId | null;
    payload: T;
    protocolVersion?: number;
    signature: Uint8Array;
    type: string;
    wallTime: number;
}

// @public
export interface ChangeHandler<T = unknown> {
    canHandle(change: Change<unknown>): boolean;
    maxVersion: number;
    minVersion: number;
    process(change: Change<T>, context: HandlerContext): Promise<void>;
    type: string;
    validate(change: Change<T>): ValidationResult;
}

// @public
export class ChangeHandlerRegistry {
    canProcess(change: Change<unknown>): boolean;
    clear(): void;
    getHandler(change: Change<unknown>): ChangeHandler<unknown> | null;
    getHandlersForType(type: string): ChangeHandler<unknown>[];
    getTypes(): string[];
    onInvalidChange(listener: (change: Change<unknown>, errors: string[]) => void): () => void;
    onUnknownType(listener: (change: Change<unknown>) => void): () => void;
    process(change: Change<unknown>, context: HandlerContext): Promise<void>;
    register<T>(handler: ChangeHandler<T>): void;
    unregister(type: string): boolean;
}

// @public
export const changeHandlerRegistry: ChangeHandlerRegistry;

// @public
export interface ChangeSerializer {
    canDeserialize(data: unknown): boolean;
    deserialize<T = unknown>(data: SerializedChange): DeserializeOutcome<T>;
    readonly name: string;
    serialize<T>(change: Change<T>): SerializedChange;
    readonly version: number;
}

// @public
export type ChangeSigner = <T>(unsigned: UnsignedChange<T>) => Promise<Change<T>>;

// @public
export function checkAndLogDeprecations(context: DeprecationContext): DeprecationWarning[];

// @public
export function checkDeprecations(context: DeprecationContext): DeprecationWarning[];

// @public
export function chunkUpdate(update: Uint8Array, chunkSize?: number): Uint8Array[];

// @public
export function clearLoggedDeprecations(): void;

// @public
export type ClientIdAttestation = ClientIdAttestationV1 | ClientIdAttestationV2;

// @public @deprecated
export interface ClientIdAttestationV1 {
    clientId: number;
    did: string;
    expiresAt: number;
    room: string;
    signature: Uint8Array;
}

// @public
export interface ClientIdAttestationV2 {
    clientId: number;
    did: DID;
    expiresAt?: number;
    room: string;
    signature: UnifiedSignature;
    timestamp: number;
    v: 2;
}

// @public
export interface ClientIdAttestationWire {
    // (undocumented)
    c: number;
    // (undocumented)
    d: string;
    // (undocumented)
    e?: number;
    // (undocumented)
    r: string;
    // (undocumented)
    s: SignatureWire;
    // (undocumented)
    t: number;
    // (undocumented)
    v: 2;
}

// @public
export interface ClientIdMap {
    cleanup(): void;
    getAll(): Array<{
        clientId: number;
        did: string;
        expiresAt: number;
    }>;
    getClientId(did: string): number | undefined;
    getOwner(clientId: number): string | undefined;
    has(clientId: number): boolean;
    register(attestation: ClientIdAttestation): void;
    size(): number;
}

// @public
export class ClientIdMapImpl implements ClientIdMap {
    activeCount(): number;
    // (undocumented)
    cleanup(): void;
    clear(): void;
    // (undocumented)
    getAll(): Array<{
        clientId: number;
        did: string;
        expiresAt: number;
    }>;
    // (undocumented)
    getClientId(did: string): number | undefined;
    // (undocumented)
    getOwner(clientId: number): string | undefined;
    // (undocumented)
    has(clientId: number): boolean;
    // (undocumented)
    register(attestation: ClientIdAttestation): void;
    // (undocumented)
    size(): number;
}

// @public
export const COMPACTION_TIME_THRESHOLD = 3600000;

// @public
export const COMPACTION_UPDATE_THRESHOLD = 100;

// @public
export function compareLamportTimestamps(a: LamportTimestamp, b: LamportTimestamp): -1 | 0 | 1;

// @public
export function computeChangeHash<T>(unsigned: UnsignedChange<T>): ContentId;

// @public
export function configureDeprecationPolicy(options: Partial<typeof DEPRECATION_POLICY>): void;

// @public (undocumented)
export interface ContentKeyProvider {
    // (undocumented)
    getOrUnwrap(nodeId: string): Promise<Uint8Array>;
}

// @public
export interface CreateAttestationOptions {
    expiresInMs?: number;
    level?: SecurityLevel;
}

// @public
export function createBatchId(): string;

// @public
export function createChangeId(): string;

// @public
export interface CreateChangeOptions<T> {
    // (undocumented)
    authorDID: DID;
    // (undocumented)
    batchId?: string;
    // (undocumented)
    batchIndex?: number;
    // (undocumented)
    batchSize?: number;
    // (undocumented)
    id: string;
    // (undocumented)
    lamport: number;
    // (undocumented)
    parentHash: ContentId | null;
    // (undocumented)
    payload: T;
    // (undocumented)
    type: string;
    // (undocumented)
    wallTime?: number;
}

// @public
export function createClientIdAttestation(clientId: number, did: string, privateKey: Uint8Array, room: string, ttlSeconds?: number): ClientIdAttestationV1;

// @public
export function createClientIdAttestation(clientId: number, room: string, keyBundle: HybridKeyBundle, options?: CreateAttestationOptions): ClientIdAttestationV2;

// @public @deprecated
export function createClientIdAttestationV1(clientId: number, did: string, privateKey: Uint8Array, room: string, ttlSeconds?: number): ClientIdAttestationV1;

// @public
export function createClientIdAttestationV2(clientId: number, room: string, keyBundle: HybridKeyBundle, options?: CreateAttestationOptions): ClientIdAttestationV2;

// @public
export interface CreateEnvelopeOptions {
    level?: SecurityLevel;
}

// @public
export function createHandler<T>(type: string, process: (change: Change<T>, context: HandlerContext) => Promise<void>, validate?: (change: Change<T>) => ValidationResult): ChangeHandler<T>;

// @public
export function createIntegrityMonitor(config: IntegrityMonitorConfig): IntegrityMonitor;

// @public
export function createLamportClock(author: DID): LamportClock;

// @public
export function createLocalCapabilities(peerId: string, features?: FeatureFlag[], options?: {
    packageVersion?: string;
    minProtocolVersion?: number;
    schemas?: string[];
}): PeerCapabilities;

// @public
export function createPersistedDocState(state: Uint8Array, updateCount?: number): PersistedDocState;

// @public
export function createReactIntegrityMonitor(options: ReactIntegrityMonitorOptions): IntegrityMonitor;

// @public
export function createSecurityPolicy(options?: Partial<SecurityPolicy>): SecurityPolicy;

// @public
export function createSerializerRegistry(defaultVersion?: number, serializers?: ChangeSerializer[]): SerializerRegistry;

// @public (undocumented)
export function createSyncLifecycleState(input: SyncLifecycleInput, previous?: SyncLifecycleState): SyncLifecycleState;

// @public
export function createTestContext(overrides?: Partial<HandlerContext>): HandlerContext;

// @public
export function createUnsignedChange<T>(options: CreateChangeOptions<T>): UnsignedChange<T>;

// @public
export function createUnsignedYjsChange(options: Omit<CreateYjsChangeOptions, 'privateKey'>): UnsignedYjsChange;

// @public
export function createVersionedHandler<T>(type: string, minVersion: number, maxVersion: number, process: (change: Change<T>, context: HandlerContext) => Promise<void>, validate?: (change: Change<T>) => ValidationResult): ChangeHandler<T>;

// @public
export function createWebCryptoChangeSigner(signingKey: Uint8Array): ChangeSigner | null;

// @public
export function createYjsChange(options: CreateYjsChangeOptions): YjsChange;

// @public
export interface CreateYjsChangeOptions {
    authorDID: string;
    clientId: number;
    lamport: number;
    nodeId: string;
    parentHash: ContentId | null;
    privateKey: Uint8Array;
    update: Uint8Array;
    updateCount: number;
    wallTime?: number;
}

// @public
export const CURRENT_PROTOCOL_VERSION = 4;

// @public (undocumented)
export function decryptYjsState(encrypted: EncryptedYjsState, contentKey: Uint8Array): Uint8Array;

// @public
export const DEFAULT_BATCHER_CONFIG: YjsBatcherConfig;

// @public
export const DEFAULT_RATE_LIMITER_CONFIG: RateLimiterConfig;

// @public
export const DEFAULT_SECURITY_POLICY: SecurityPolicy;

// @public
export const DEFAULT_YJS_SCORING_CONFIG: YjsScoringConfig;

// @public
export const defaultNegotiator: VersionNegotiator;

// @public
export const DEPRECATION_POLICY: {
    minimumDeprecationPeriodDays: number;
    logWarnings: boolean;
    strictMode: boolean;
    logger: (message: string) => void;
};

// @public
export type DeprecationCallback = (warning: DeprecationWarning) => void;

// @public
export interface DeprecationContext {
    features?: (FeatureFlag | string)[];
    packageVersion?: string;
    protocolVersion?: number;
    // Warning: (ae-forgotten-export) The symbol "SchemaIRI" needs to be exported by the entry point index.d.ts
    schemas?: (SchemaIRI | string)[];
}

// @public
export class DeprecationError extends Error {
    constructor(warning: DeprecationWarning);
    // (undocumented)
    readonly warning: DeprecationWarning;
}

// @public
export interface DeprecationNotice {
    alternative?: string;
    deprecatedDate?: string;
    deprecatedIn: string;
    description: string;
    migrationGuide?: string;
    removedIn?: string;
    subject: string;
    sunsetDate?: string;
    type: DeprecationType;
}

// @public
export const DEPRECATIONS: DeprecationNotice[];

// @public
export type DeprecationType = 'protocol' | 'schema' | 'feature' | 'api';

// @public
export interface DeprecationWarning {
    action: string;
    daysUntilRemoval?: number;
    message: string;
    notice: DeprecationNotice;
    severity: 'warning' | 'error';
}

// @public
export function deriveClientIdFromDid(did: string): number;

// @public (undocumented)
export function deriveSyncLifecyclePhase(input: SyncLifecycleInput): SyncLifecyclePhase;

// @public
export function deserializeClientIdAttestation(wire: ClientIdAttestationWire): ClientIdAttestationV2;

// @public (undocumented)
export function deserializeEncryptedYjsState(bytes: Uint8Array): EncryptedYjsState;

// @public (undocumented)
export interface DeserializeError {
    // (undocumented)
    error: string;
    rawData?: unknown;
    // (undocumented)
    success: false;
}

// @public (undocumented)
export type DeserializeOutcome<T = unknown> = DeserializeResult<T> | DeserializeError;

// @public
export interface DeserializeResult<T = unknown> {
    // (undocumented)
    change: Change<T>;
    // (undocumented)
    success: true;
}

// @public
export function deserializeYjsEnvelope(wire: SignedYjsEnvelopeWire): SignedYjsEnvelopeV2;

// @public
export function detectFork<T>(changes: Change<T>[]): {
    hasFork: boolean;
    forkPoints: ContentId[];
};

// @public
export function diffFeatures(a: readonly FeatureFlag[], b: readonly FeatureFlag[] | readonly string[]): FeatureFlag[];

// @public
export interface EncryptedYjsState {
    // (undocumented)
    checkpointedAt: number;
    // (undocumented)
    encryptedState: Uint8Array;
    // (undocumented)
    nodeId: string;
    // (undocumented)
    nonce: Uint8Array;
    // (undocumented)
    stateHash: Uint8Array;
    // (undocumented)
    stateVector: Uint8Array;
    // (undocumented)
    updatesSinceCheckpoint: number;
    // (undocumented)
    version: 1;
}

// @public (undocumented)
export function encryptYjsState(state: Uint8Array, nodeId: string, contentKey: Uint8Array, options?: {
    stateVector?: Uint8Array;
    checkpointedAt?: number;
    updatesSinceCheckpoint?: number;
}): EncryptedYjsState;

// @public
export function envelopeSize(envelope: SignedYjsEnvelopeV2): number;

// @public
export interface EnvelopeVerificationResult {
    // (undocumented)
    authorDID: DID;
    // (undocumented)
    clientId: number;
    // (undocumented)
    errors: string[];
    // (undocumented)
    level: SecurityLevel;
    // (undocumented)
    valid: boolean;
}

// @public @deprecated
export interface EnvelopeVerifyResult {
    // (undocumented)
    reason?: 'invalid_signature' | 'did_resolution_failed' | 'update_too_large';
    // (undocumented)
    valid: boolean;
}

// @public
export function estimateBase64DecodedLength(value: string): number;

// @public
export interface FeatureConfig {
    conflicts?: FeatureFlag[];
    description: string;
    required: boolean;
    requires?: FeatureFlag[];
    since: number;
}

// @public
export type FeatureFlag = 'node-changes' | 'yjs-updates' | 'signed-yjs-envelopes' | 'batch-changes' | 'lamport-ordering' | 'hash-chains' | 'schema-versioning' | 'capability-negotiation' | 'peer-scoring' | 'schema-lenses' | 'unknown-preservation' | 'schema-inheritance' | 'federated-queries' | 'compressed-payloads';

// @public
export const FEATURES: Record<FeatureFlag, FeatureConfig>;

// @public (undocumented)
export interface FeatureValidationError {
    // (undocumented)
    feature: FeatureFlag;
    // (undocumented)
    message: string;
    // (undocumented)
    relatedFeature?: FeatureFlag;
    // (undocumented)
    type: 'missing-dependency' | 'conflict' | 'version-mismatch' | 'missing-required';
}

// @public
export interface FeatureValidationResult {
    // (undocumented)
    errors: FeatureValidationError[];
    // (undocumented)
    valid: boolean;
    // (undocumented)
    warnings: FeatureValidationWarning[];
}

// @public (undocumented)
export interface FeatureValidationWarning {
    // (undocumented)
    feature: FeatureFlag;
    // (undocumented)
    message: string;
    // (undocumented)
    type: 'deprecated' | 'experimental';
}

// @public
export function findCommonAncestor<T>(a: Change<T>, b: Change<T>, allChanges: Change<T>[]): Change<T> | null;

// @public
export function findHeads(changes: Change<unknown>[]): Change<unknown>[];

// @public
export function findOrphans(changes: Change<unknown>[]): Change<unknown>[];

// @public
export function findRoots(changes: Change<unknown>[]): Change<unknown>[];

// @public
export interface Fork<T = unknown> {
    branch1: Change<T>[];
    branch2: Change<T>[];
    commonAncestor: ContentId;
}

// @public
export function formatDeprecationReport(warnings: DeprecationWarning[]): string;

// @public
export function formatIntegrityReport(report: IntegrityReport): string;

// @public
export function getAllDependencies(feature: FeatureFlag, visited?: Set<FeatureFlag>): FeatureFlag[];

// @public
export function getAncestry<T>(change: Change<T>, allChanges: Change<T>[]): Change<T>[];

// @public
export function getChainDepth(changes: Change<unknown>[]): number;

// @public
export function getChainHeads<T>(changes: Change<T>[]): Change<T>[];

// @public
export function getChainRoots<T>(changes: Change<T>[]): Change<T>[];

// @public
export function getChangeNodeId(change: Change<unknown>): string | undefined;

// @public
export function getDefaultSerializer(): ChangeSerializer;

// @public
export function getDeprecation(subject: string): DeprecationNotice | undefined;

// @public
export function getDeprecationsByType(type: DeprecationType): DeprecationNotice[];

// @public
export function getEnabledFeatures(protocolVersion: number): FeatureFlag[];

// @public
export function getFeatureConflicts(feature: FeatureFlag): FeatureFlag[];

// @public
export function getFeatureDependencies(feature: FeatureFlag): FeatureFlag[];

// @public
export function getFeatureVersion(feature: FeatureFlag): number;

// @public
export function getForks<T>(changes: Change<T>[]): Fork<T>[];

// @public
export function getOptionalFeatures(protocolVersion: number): FeatureFlag[];

// @public
export function getRequiredFeatures(protocolVersion: number): FeatureFlag[];

// @public
export function getSecurityLevel(operationType: string, policy?: SecurityPolicy): SecurityLevel;

// @public
export function getSerializer(version: number): ChangeSerializer | undefined;

// @public (undocumented)
export interface GrantEventStore {
    // Warning: (ae-forgotten-export) The symbol "GrantEvent" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    subscribe(listener: (event: GrantEvent) => void): () => void;
}

// @public
export interface HandlerContext {
    authorDID?: string;
    emit: (event: string, data: Record<string, unknown>) => void;
    storeUnknown: (change: Change<unknown>) => Promise<void>;
}

// @public
export type HandlerEvent = {
    type: 'unknownChangeType';
    change: Change<unknown>;
} | {
    type: 'invalidChange';
    change: Change<unknown>;
    errors: string[];
} | {
    type: 'processed';
    change: Change<unknown>;
    duration: number;
};

// @public
export function hashYjsState(state: Uint8Array): string;

// @public
export function hasSignedEnvelope(msg: unknown): msg is {
    envelope: SignedYjsEnvelope;
    [key: string]: unknown;
};

// @public
export const HYBRID_SECURITY_POLICY: SecurityPolicy;

// @public (undocumented)
export function inferReplicationNamespaceKind(namespace: string): ReplicationNamespaceKind;

// @public
export interface IntegrityIssue {
    changeId: string;
    details: string;
    repairAction?: RepairAction;
    severity: 'error' | 'warning';
    type: IntegrityIssueType;
}

// @public
export type IntegrityIssueType = 'hash-mismatch' | 'signature-invalid' | 'chain-broken' | 'missing-parent' | 'duplicate-id' | 'invalid-lamport' | 'future-timestamp';

// @public
export interface IntegrityMonitor {
    checkNow(): Promise<IntegrityReport>;
    configure(config: Partial<IntegrityMonitorConfig>): void;
    getStats(): IntegrityMonitorStats;
    isRunning(): boolean;
    start(): void;
    stop(): void;
}

// @public
export interface IntegrityMonitorConfig {
    checkOnStart?: boolean;
    debug?: boolean;
    getChanges: () => Promise<Change<unknown>[]> | Change<unknown>[];
    intervalMs?: number;
    minChangesForCheck?: number;
    onCheck?: (report: IntegrityReport) => void;
    onError?: (error: Error) => void;
    onIssues?: (report: IntegrityReport) => void;
    quickCheck?: boolean;
    verifyOptions?: VerifyOptions;
}

// @public
export interface IntegrityMonitorStats {
    checksPerformed: number;
    isChecking: boolean;
    isRunning: boolean;
    lastCheckAt: Date | null;
    lastCheckDurationMs: number;
    lastReport: IntegrityReport | null;
    totalIssuesFound: number;
}

// @public
export interface IntegrityReport {
    checked: number;
    durationMs: number;
    issues: IntegrityIssue[];
    repairable: boolean;
    summary: {
        errors: number;
        warnings: number;
        byType: Record<IntegrityIssueType, number>;
    };
    valid: number;
}

// @public
export function intersectFeatures(local: readonly FeatureFlag[], remote: readonly FeatureFlag[] | readonly string[]): FeatureFlag[];

// @public
export function isAfter(a: LamportTimestamp, b: LamportTimestamp): boolean;

// @public
export function isAwarenessUpdateTooLarge(update: Uint8Array, maxSize?: number): boolean;

// @public
export function isBase64PayloadTooLarge(value: string, maxSize: number): boolean;

// @public
export function isBefore(a: LamportTimestamp, b: LamportTimestamp): boolean;

// @public
export function isCriticalOperation(operationType: string): boolean;

// @public
export function isDeprecated(subject: string): boolean;

// @public
export function isDerivedClientId(clientId: number, did: string): boolean;

// @public
export function isDocumentTooLarge(state: Uint8Array, maxSize?: number): boolean;

// @public
export function isEphemeralOperation(operationType: string): boolean;

// @public
export function isFeatureAvailable(feature: FeatureFlag, protocolVersion: number): boolean;

// @public
export function isFeatureEnabled(feature: FeatureFlag, enabledFeatures: readonly FeatureFlag[] | readonly string[]): boolean;

// @public
export function isLegacyUpdate(msg: unknown): msg is {
    data: Uint8Array;
    [key: string]: unknown;
};

// @public
export function isNodeChange(change: Change<unknown>): boolean;

// @public
export function isRemoved(subject: string): boolean;

// @public
export function isStateVectorTooLarge(stateVector: Uint8Array, maxSize?: number): boolean;

// @public
export function isUpdateTooLarge(update: Uint8Array, maxSize?: number): boolean;

// @public
export function isV1Attestation(attestation: ClientIdAttestation): attestation is ClientIdAttestationV1;

// @public
export function isV1Envelope(envelope: SignedYjsEnvelope): envelope is SignedYjsEnvelopeV1;

// @public
export function isV2Attestation(attestation: ClientIdAttestation): attestation is ClientIdAttestationV2;

// @public
export function isV2Envelope(envelope: SignedYjsEnvelope): envelope is SignedYjsEnvelopeV2;

// @public
export function isYjsChange(change: Change<unknown>): change is YjsChange;

// @public
export interface LamportClock {
    author: DID;
    time: number;
}

// @public
export interface LamportTimestamp {
    author: DID;
    time: number;
}

// @public
export function loadVerifiedState(docId: string, record: Partial<PersistedDocState> & {
    state: Uint8Array;
}): Uint8Array;

// @public
export function logDeprecation(warning: DeprecationWarning): void;

// @public
export const MAX_SECURITY_POLICY: SecurityPolicy;

// @public
export const MAX_YJS_AWARENESS_UPDATE_SIZE = 65536;

// @public
export const MAX_YJS_DOC_SIZE = 52428800;

// @public
export const MAX_YJS_STATE_VECTOR_SIZE = 65536;

// @public
export const MAX_YJS_UPDATE_SIZE = 1048576;

// @public
export const MAX_YJS_UPDATES_PER_MINUTE = 600;

// @public
export const MAX_YJS_UPDATES_PER_SECOND = 30;

// @public
export function maxTime(timestamps: LamportTimestamp[]): number;

// @public
export function mergeSecurityPolicies(...policies: Partial<SecurityPolicy>[]): SecurityPolicy;

// @public
export type MergeUpdatesFn = (updates: Uint8Array[]) => Uint8Array;

// @public
export interface NegotiatedSession {
    agreedVersion: number;
    canUse(feature: FeatureFlag): boolean;
    commonFeatures: FeatureFlag[];
    peerId: string;
    // (undocumented)
    success: true;
    warnings: NegotiationWarning[];
}

// @public
export interface NegotiationFailure {
    error: 'incompatible-versions' | 'missing-required-features' | 'invalid-capabilities';
    localVersion: number;
    message: string;
    remoteVersion: number;
    // (undocumented)
    success: false;
    suggestion: 'upgrade-client' | 'upgrade-hub' | 'upgrade-both' | 'contact-support';
}

// @public
export type NegotiationResult = NegotiatedSession | NegotiationFailure;

// @public
export interface NegotiationWarning {
    affectedFeatures?: FeatureFlag[];
    // (undocumented)
    message: string;
    // (undocumented)
    type: 'degraded-features' | 'version-mismatch' | 'unknown-features';
}

// @public (undocumented)
export function normalizeSyncFederationHubs(config: SyncReplicationConfig | undefined, fallbackUrls?: string[]): SyncFederationHub[];

// @public
export type OperationType = 'cursor-update' | 'presence-update' | 'typing-indicator' | 'viewport-update' | 'awareness-update' | 'node-create' | 'node-update' | 'node-delete' | 'yjs-update' | 'comment-add' | 'key-rotation' | 'permission-grant' | 'permission-revoke' | 'identity-recovery' | 'share-create';

// @public
export function parseCapabilities(message: unknown): PeerCapabilities | null;

// @public
export function parseTimestamp(serialized: string): LamportTimestamp;

// @public
export type PeerAction = 'allow' | 'warn' | 'throttle' | 'block';

// @public
export interface PeerCapabilities {
    features: FeatureFlag[];
    minProtocolVersion: number;
    packageVersion: string;
    peerId: string;
    protocolVersion: number;
    schemas?: string[];
}

// @public
export interface PeerInfo {
    capabilities?: PeerCapabilities;
    connectedAt: number;
    id: string;
    lastSeen: number;
    name?: string;
    negotiatedSession?: NegotiatedSession;
}

// @public
export interface PersistedDocState {
    hash: string;
    persistedAt: number;
    state: Uint8Array;
    updateCount: number;
}

// @public (undocumented)
export function planReplicationDestinations(input: {
    namespace: string;
    config?: SyncReplicationConfig;
    fallbackHubUrls?: string[];
}): ReplicationPlan;

// @public (undocumented)
export type PolicyRevisionSimulation = {
    before: ReplicationPlan;
    after: ReplicationPlan;
    addedHubIds: string[];
    removedHubIds: string[];
    retainedHubIds: string[];
    changed: boolean;
};

// @public
export interface ProcessResult {
    duration: number;
    errors?: string[];
    handlerType?: string;
    success: boolean;
}

// @public
export function quickIntegrityCheck(changes: Change<unknown>[]): Promise<IntegrityReport>;

// @public
export interface RateLimiterConfig {
    burstAllowance: number;
    maxPerMinute: number;
    maxPerSecond: number;
}

// @public
export interface ReactIntegrityMonitorOptions extends IntegrityMonitorConfig {
    onStateChange?: (stats: IntegrityMonitorStats) => void;
}

// @public
export function reassembleChunks(chunks: Uint8Array[]): Uint8Array;

// @public
export function receive(clock: LamportClock, receivedTime: number): LamportClock;

// @public (undocumented)
export interface RecipientKeyResolver {
    // (undocumented)
    resolveBatch(dids: DID[]): Promise<Map<DID, Uint8Array>>;
}

// @public
export function recomputeChangeHash<T>(change: Change<T>): ContentId;

// @public
export function registerDeprecation(notice: DeprecationNotice): void;

// @public
export interface RegistryStats {
    handlerCount: number;
    typeCount: number;
    versionedTypes: string[];
}

// @public
export interface RepairAction {
    automatic: boolean;
    // (undocumented)
    description: string;
    // (undocumented)
    type: RepairActionType;
}

// @public
export type RepairActionType = 'recompute-hash' | 'request-from-peers' | 'remove-duplicate' | 'mark-orphan' | 'none';

// @public
export type ReplicationNamespaceKind = 'system' | 'user';

// @public (undocumented)
export type ReplicationPlan = {
    namespace: string;
    kind: ReplicationNamespaceKind;
    policy: SyncFederationNamespacePolicy | null;
    destinations: ReplicationPlanDestination[];
    diagnostics: ReplicationPlanDiagnostic[];
    trace: ReplicationPlanTraceStep[];
};

// @public (undocumented)
export type ReplicationPlanDestination = {
    hubId: string;
    url: string;
    priority: number;
    reason: string;
};

// @public (undocumented)
export type ReplicationPlanDiagnostic = {
    code: 'no_hubs_configured' | 'policy_hub_not_found' | 'minimum_hubs_not_satisfied' | 'hub_kind_mismatch' | 'hub_disabled';
    message: string;
    hubId?: string;
};

// @public (undocumented)
export type ReplicationPlanTraceStep = {
    step: string;
    message: string;
    hubId?: string;
    namespace?: string;
};

// @public (undocumented)
export interface ResolvedSyncReplicationPolicy {
    allowUnsignedReplication: boolean;
    requireSignedReplication: boolean;
}

// @public (undocumented)
export function resolveSyncReplicationPolicy(config: SyncReplicationConfig | undefined): ResolvedSyncReplicationPolicy;

// @public
export interface SecurityPolicy {
    default: SecurityLevel;
    overrides: Record<string, SecurityLevel>;
}

// @public
export function serializeClientIdAttestation(attestation: ClientIdAttestationV2): ClientIdAttestationWire;

// @public
export type SerializedChange = Uint8Array | Record<string, unknown>;

// @public (undocumented)
export function serializeEncryptedYjsState(state: EncryptedYjsState): Uint8Array;

// @public
export interface SerializeOptions {
    binary?: boolean;
    includeDefaults?: boolean;
}

// @public
export interface SerializerRegistry {
    detect(data: unknown): ChangeSerializer | undefined;
    get(version: number): ChangeSerializer | undefined;
    getDefault(): ChangeSerializer;
    getVersions(): number[];
    register(serializer: ChangeSerializer): void;
}

// Warning: (ae-forgotten-export) The symbol "DefaultSerializerRegistry" needs to be exported by the entry point index.d.ts
//
// @public
export const serializerRegistry: DefaultSerializerRegistry;

// @public
export function serializeTimestamp(ts: LamportTimestamp): string;

// @public
export function serializeYjsEnvelope(envelope: SignedYjsEnvelopeV2): SignedYjsEnvelopeWire;

// @public
export function shouldCompact(updateCount: number, persistedAt: number): boolean;

// @public
export function signChange<T>(unsigned: UnsignedChange<T>, signingKey: Uint8Array): Change<T>;

// @public
export type SignedYjsEnvelope = SignedYjsEnvelopeV1 | SignedYjsEnvelopeV2;

// @public @deprecated
export interface SignedYjsEnvelopeV1 {
    authorDID: string;
    clientId: number;
    signature: Uint8Array;
    timestamp: number;
    update: Uint8Array;
}

// @public
export interface SignedYjsEnvelopeV2 {
    meta: {
        authorDID: DID;
        clientId: number;
        timestamp: number;
        docId: string;
    };
    signature: UnifiedSignature;
    update: Uint8Array;
    v: 2;
}

// @public
export interface SignedYjsEnvelopeWire {
    m: {
        a: string;
        c: number;
        t: number;
        d: string;
    };
    s: SignatureWire;
    u: string;
    // (undocumented)
    v: 2;
}

// @public
export function signYjsUpdate(update: Uint8Array, authorDID: string, privateKey: Uint8Array, clientId: number): SignedYjsEnvelopeV1;

// @public
export function signYjsUpdate(update: Uint8Array, docId: string, clientId: number, keyBundle: HybridKeyBundle, options?: CreateEnvelopeOptions): SignedYjsEnvelopeV2;

// @public
export function signYjsUpdateBatch(updates: Uint8Array[], docId: string, clientId: number, keyBundle: HybridKeyBundle, options?: CreateEnvelopeOptions): SignedYjsEnvelopeV2[];

// @public @deprecated
export function signYjsUpdateV1(update: Uint8Array, authorDID: string, privateKey: Uint8Array, clientId: number): SignedYjsEnvelopeV1;

// @public
export function signYjsUpdateV2(update: Uint8Array, docId: string, clientId: number, keyBundle: HybridKeyBundle, options?: CreateEnvelopeOptions): SignedYjsEnvelopeV2;

// @public (undocumented)
export function simulateSyncPolicyRevision(input: {
    namespace: string;
    current?: SyncReplicationConfig;
    revision?: SyncReplicationConfig;
    fallbackHubUrls?: string[];
}): PolicyRevisionSimulation;

// @public (undocumented)
export interface SyncCompatibilityConfig {
    allowUnsignedReplication?: boolean;
}

// @public
export type SyncConnectionStatus = 'disconnected' | 'connecting' | 'connected' | 'error';

// @public
export type SyncEventListener<T, E extends keyof SyncProviderEvents<T>> = SyncProviderEvents<T>[E];

// @public (undocumented)
export type SyncFederationConfig = {
    hubs?: readonly SyncFederationHub[];
    namespacePolicies?: readonly SyncFederationNamespacePolicy[];
    defaultSystemHubIds?: readonly string[];
    defaultUserHubIds?: readonly string[];
};

// @public (undocumented)
export type SyncFederationHub = {
    id: string;
    url: string;
    priority?: number;
    kinds?: readonly ReplicationNamespaceKind[];
    disabled?: boolean;
};

// @public (undocumented)
export type SyncFederationNamespacePolicy = {
    namespace: string;
    kind?: ReplicationNamespaceKind;
    includeHubIds?: readonly string[];
    excludeHubIds?: readonly string[];
    minHubs?: number;
    maxHubs?: number;
};

// @public (undocumented)
export type SyncLifecycleInput = {
    started: boolean;
    stopped: boolean;
    localReady: boolean;
    everConnected: boolean;
    connectionStatus: SyncConnectionStatus;
    replaying?: boolean;
};

// @public (undocumented)
export type SyncLifecyclePhase = 'idle' | 'starting' | 'local-ready' | 'connecting' | 'healthy' | 'degraded' | 'replaying' | 'stopped';

// @public (undocumented)
export type SyncLifecycleState = {
    phase: SyncLifecyclePhase;
    connectionStatus: SyncConnectionStatus;
    replaying: boolean;
    lastTransitionAt: number;
};

// @public
export interface SyncProvider<T = unknown> {
    broadcast(change: Change<T>): Promise<void>;
    canUseFeature(peerId: string, feature: FeatureFlag): boolean;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    getNegotiatedSession(peerId: string): NegotiatedSession | undefined;
    readonly localCapabilities: PeerCapabilities;
    off<E extends keyof SyncProviderEvents<T>>(event: E, listener: SyncProviderEvents<T>[E]): void;
    on<E extends keyof SyncProviderEvents<T>>(event: E, listener: SyncProviderEvents<T>[E]): void;
    once<E extends keyof SyncProviderEvents<T>>(event: E, listener: SyncProviderEvents<T>[E]): void;
    readonly peerInfo: Map<string, PeerInfo>;
    readonly peers: string[];
    requestChanges(peerId: string, since?: string): Promise<Change<T>[]>;
    requestChangesFromAll(since?: string): Promise<Change<T>[]>;
    readonly status: SyncStatus;
}

// @public
export interface SyncProviderEvents<T = unknown> {
    'capability-degraded': (peerId: string, warnings: string[]) => void;
    'change-received': (change: Change<T>, peerId: string) => void;
    'changes-synced': (changes: Change<T>[]) => void;
    'negotiation-complete': (peerId: string, session: NegotiatedSession) => void;
    'negotiation-failed': (peerId: string, error: string, suggestion: string) => void;
    'peer-connected': (peer: PeerInfo) => void;
    'peer-disconnected': (peerId: string) => void;
    'status-change': (status: SyncStatus) => void;
    'unknown-change-type': (change: Change<unknown>, peerId: string) => void;
    error: (error: Error) => void;
}

// @public
export interface SyncProviderOptions {
    autoReconnect?: boolean;
    enabledFeatures?: FeatureFlag[];
    localDID?: string;
    maxReconnectAttempts?: number;
    minProtocolVersion?: number;
    packageVersion?: string;
    reconnectDelay?: number;
    room: string;
    signalingServers?: string[];
    strictVersionCheck?: boolean;
    timeout?: number;
}

// @public (undocumented)
export interface SyncReplicationConfig {
    compatibility?: SyncCompatibilityConfig;
    federation?: SyncFederationConfig;
}

// @public
export type SyncStatus = 'disconnected' | 'connecting' | 'synced' | 'syncing' | 'error';

// @public
export function tick(clock: LamportClock): [LamportClock, LamportTimestamp];

// @public (undocumented)
export function toEncryptedData(state: EncryptedYjsState): EncryptedData;

// @public
export function topologicalSort<T>(changes: Change<T>[]): Change<T>[];

// @public
export interface UnsignedChange<T = unknown> {
    // (undocumented)
    authorDID: DID;
    // (undocumented)
    batchId?: string;
    // (undocumented)
    batchIndex?: number;
    // (undocumented)
    batchSize?: number;
    // (undocumented)
    id: string;
    // (undocumented)
    lamport: number;
    // (undocumented)
    parentHash: ContentId | null;
    // (undocumented)
    payload: T;
    // (undocumented)
    protocolVersion?: number;
    // (undocumented)
    type: string;
    // (undocumented)
    wallTime: number;
}

// @public
export type UnsignedYjsChange = UnsignedChange<YjsUpdatePayload>;

// @public
export class V1Serializer implements ChangeSerializer {
    // (undocumented)
    canDeserialize(data: unknown): boolean;
    // (undocumented)
    deserialize<T = unknown>(data: SerializedChange): DeserializeOutcome<T>;
    // (undocumented)
    readonly name = "V1 JSON Serializer";
    // (undocumented)
    serialize<T>(change: Change<T>): SerializedChange;
    // (undocumented)
    readonly version = 1;
}

// @public
export const v1Serializer: V1Serializer;

// @public
export class V2Serializer implements ChangeSerializer {
    static addSchemaVersion<T extends Record<string, unknown>>(payload: T, schemaVersion: string): T & {
        _sv: string;
    };
    // (undocumented)
    canDeserialize(data: unknown): boolean;
    // (undocumented)
    deserialize<T = unknown>(data: SerializedChange): DeserializeOutcome<T>;
    static getSchemaVersion(payload: unknown): string | undefined;
    // (undocumented)
    readonly name = "V2 Compact Serializer";
    // (undocumented)
    serialize<T>(change: Change<T>): SerializedChange;
    // (undocumented)
    readonly version = 2;
}

// @public
export const v2Serializer: V2Serializer;

// @public
export function validateChain<T>(changes: Change<T>[]): ChainValidationResult;

// @public
export function validateClientIdOwnership(clientId: number, authorDID: string, map: ClientIdMap): boolean;

// @public
export function validateFeatureSet(features: readonly FeatureFlag[], protocolVersion?: number): FeatureValidationResult;

// @public
export interface ValidationError {
    code: string;
    message: string;
    path?: string;
}

// @public
export interface ValidationResult {
    errors: string[];
    valid: boolean;
}

// @public
export interface ValidationWarning {
    code: string;
    message: string;
    path?: string;
}

// @public
export interface VerifyAttestationOptions {
    enforceDerivedClientId?: boolean;
    minLevel?: SecurityLevel;
    registry?: PQKeyRegistry;
}

// @public
export function verifyChange<T>(change: Change<T>, publicKey: Uint8Array): boolean;

// @public
export function verifyChangeHash<T>(change: Change<T>): boolean;

// @public
export function verifyClientIdAttestation(attestation: ClientIdAttestationV1): AttestationVerifyResult;

// @public (undocumented)
export function verifyClientIdAttestation(attestation: ClientIdAttestationV2, options?: VerifyAttestationOptions): Promise<AttestationVerificationResult>;

// @public @deprecated
export function verifyClientIdAttestationV1(attestation: ClientIdAttestationV1): AttestationVerifyResult;

// @public
export function verifyClientIdAttestationV2(attestation: ClientIdAttestationV2, options?: VerifyAttestationOptions): Promise<AttestationVerificationResult>;

// @public
export interface VerifyEnvelopeOptions {
    expectedDocId?: string;
    maxAge?: number;
    minLevel?: SecurityLevel;
    policy?: 'strict' | 'permissive';
    registry?: PQKeyRegistry;
}

// @public
export function verifyIntegrity(changes: Change<unknown>[], options?: VerifyOptions): Promise<IntegrityReport>;

// @public
export interface VerifyOptions {
    maxFutureTimestamp?: number;
    onProgress?: (checked: number, total: number) => void;
    resolveKey?: (did: string) => Uint8Array;
    skipChain?: boolean;
    skipHashes?: boolean;
    skipSignatures?: boolean;
}

// @public
export function verifyPersistedDocState(docId: string, record: PersistedDocState): void;

// @public
export function verifySingleChange(change: Change<unknown>, options?: VerifyOptions): Promise<{
    valid: boolean;
    issues: IntegrityIssue[];
}>;

// @public
export function verifyYjsEnvelope(envelope: SignedYjsEnvelopeV1): EnvelopeVerifyResult;

// @public (undocumented)
export function verifyYjsEnvelope(envelope: SignedYjsEnvelopeV2, options?: VerifyEnvelopeOptions): Promise<EnvelopeVerificationResult>;

// @public
export function verifyYjsEnvelopeQuick(envelope: SignedYjsEnvelopeV2, options?: VerifyEnvelopeOptions): Promise<boolean>;

// @public @deprecated
export function verifyYjsEnvelopeV1(envelope: SignedYjsEnvelopeV1): EnvelopeVerifyResult;

// @public
export function verifyYjsEnvelopeV2(envelope: SignedYjsEnvelopeV2, options?: VerifyEnvelopeOptions): Promise<EnvelopeVerificationResult>;

// @public
export function verifyYjsStateIntegrity(state: Uint8Array, expectedHash: string): boolean;

// @public
export class VersionNegotiator {
    negotiate(local: PeerCapabilities, remote: PeerCapabilities): NegotiationResult;
    validateCapabilities(capabilities: PeerCapabilities): {
        valid: boolean;
        errors: string[];
    };
}

// @public (undocumented)
export interface YDocCodec<TDoc extends YDocLike = YDocLike> {
    // (undocumented)
    applyUpdate(doc: TDoc, update: Uint8Array, origin?: string): void;
    // (undocumented)
    createDoc(nodeId: string): TDoc;
    // (undocumented)
    encodeStateAsUpdate(doc: TDoc): Uint8Array;
    // (undocumented)
    encodeStateVector(doc: TDoc): Uint8Array;
}

// @public (undocumented)
export interface YDocLike {
    // (undocumented)
    emit?: (event: string, args: unknown[]) => void;
}

// @public
export const YJS_CHANGE_TYPE = "yjs-update";

// @public
export const YJS_RATE_BURST_ALLOWANCE = 10;

// @public
export const YJS_SYNC_CHUNK_SIZE = 262144;

// @public (undocumented)
export interface YjsAuthDecision {
    // (undocumented)
    allowed: boolean;
    // (undocumented)
    authorDID: DID;
    // (undocumented)
    cached: boolean;
}

// @public (undocumented)
export class YjsAuthGate {
    constructor(evaluator: PolicyEvaluator, nodeId: string, options?: YjsAuthGateOptions);
    // (undocumented)
    canApplyUpdate(envelope: Pick<SignedYjsEnvelopeV2, 'meta'>): Promise<YjsAuthDecision>;
    // (undocumented)
    invalidateAll(): void;
    // (undocumented)
    invalidatePeer(did: DID): void;
}

// @public (undocumented)
export interface YjsAuthGateOptions {
    // (undocumented)
    cacheTTL?: number;
    // (undocumented)
    now?: () => number;
}

// @public
export class YjsBatcher {
    constructor(onFlush: BatchFlushCallback, config?: Partial<YjsBatcherConfig>, mergeUpdates?: MergeUpdatesFn);
    add(update: Uint8Array, isParagraphBreak?: boolean): void;
    destroy(): void;
    flush(): void;
    hasPending(): boolean;
    isDestroyed(): boolean;
    pendingCount(): number;
}

// @public
export interface YjsBatcherConfig {
    batchWindowMs: number;
    flushOnParagraph: boolean;
    maxBatchSize: number;
}

// @public
export type YjsChange = Change<YjsUpdatePayload>;

// @public (undocumented)
export class YjsCheckpointer {
    constructor(options?: YjsCheckpointerOptions);
    // (undocumented)
    checkpoint(input: {
        state: Uint8Array;
        nodeId: string;
        contentKey: Uint8Array;
        stateVector?: Uint8Array;
    }): EncryptedYjsState;
    // (undocumented)
    shouldCheckpoint(state: Pick<EncryptedYjsState, 'updatesSinceCheckpoint' | 'checkpointedAt'>): boolean;
}

// @public (undocumented)
export interface YjsCheckpointerOptions {
    // (undocumented)
    maxAgeMs?: number;
    // (undocumented)
    maxUpdates?: number;
    // (undocumented)
    now?: () => number;
}

// @public
export class YjsIntegrityError extends Error {
    constructor(docId: string, expectedHash: string, actualHash: string);
    // (undocumented)
    readonly actualHash: string;
    // (undocumented)
    readonly docId: string;
    // (undocumented)
    readonly expectedHash: string;
}

// @public
export interface YjsPeerActionEvent {
    // (undocumented)
    action: PeerAction;
    // (undocumented)
    metrics: YjsPeerMetrics;
    // (undocumented)
    peerId: string;
    // (undocumented)
    reason: YjsViolationType;
    // (undocumented)
    score: number;
}

// @public (undocumented)
export type YjsPeerActionListener = (event: YjsPeerActionEvent) => void;

// @public
export interface YjsPeerMetrics {
    firstSeen: number;
    invalidSignatures: number;
    lastViolation: number;
    oversizedUpdates: number;
    rateExceeded: number;
    unattestedClientIds: number;
    unauthorizedUpdates: number;
    unsignedUpdates: number;
    validUpdates: number;
}

// @public
export class YjsPeerScorer {
    constructor(config?: Partial<YjsScoringConfig>);
    clear(): void;
    // (undocumented)
    readonly config: YjsScoringConfig;
    getAction(score: number): PeerAction;
    getAllMetrics(): Map<string, YjsPeerMetrics>;
    getAllPeerIds(): string[];
    getMetrics(peerId: string): YjsPeerMetrics | undefined;
    getPeerAction(peerId: string): PeerAction;
    getScore(peerId: string): number;
    getViolationRatio(peerId: string): number;
    onAction(listener: YjsPeerActionListener): () => void;
    penalize(peerId: string, reason: YjsViolationType): PeerAction;
    recordValid(peerId: string): void;
    remove(peerId: string): void;
    tick(recoveryWindow?: number): void;
}

// @public
export class YjsRateLimiter {
    constructor(options?: YjsRateLimiterOptions);
    allow(peerId: string): boolean;
    cleanupStale(): number;
    clear(): void;
    destroy(): void;
    getInfo(peerId: string): {
        perSecond: number;
        perMinute: number;
    } | undefined;
    get peerCount(): number;
    remove(peerId: string): void;
    startCleanup(intervalMs?: number): void;
    stopCleanup(): void;
}

// @public
export interface YjsRateLimiterOptions extends Partial<RateLimiterConfig> {
    cleanupIntervalMs?: number;
    staleThresholdMs?: number;
}

// @public
export interface YjsScoringConfig {
    instantBlockAfter: number;
    penalties: {
        invalidSignature: number;
        oversizedUpdate: number;
        rateExceeded: number;
        unsignedUpdate: number;
        unattestedClientId: number;
        unauthorizedUpdate: number;
    };
    recoveryRate: number;
    // Warning: (ae-forgotten-export) The symbol "SyncTelemetry" needs to be exported by the entry point index.d.ts
    telemetry?: SyncTelemetry;
    thresholds: {
        warn: number;
        throttle: number;
        block: number;
    };
}

// @public (undocumented)
export class YjsStateIntegrityError extends Error {
    constructor(message?: string);
}

// @public
export interface YjsUpdatePayload {
    clientId: number;
    nodeId: string;
    update: Uint8Array;
    updateCount: number;
}

// @public
export type YjsViolationType = 'invalidSignature' | 'oversizedUpdate' | 'rateExceeded' | 'unsignedUpdate' | 'unattestedClientId' | 'unauthorizedUpdate';

// (No @packageDocumentation comment for this package)

```
