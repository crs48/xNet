## API Report File for "@xnetjs/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export function actionExpressionOrder(action: AuthAction): readonly AuthAction[];

// @public
export type ActionKey<TAuth extends AuthorizationDefinition> = keyof TAuth['actions'] & string;

// @public
export const ALL_CAPABILITIES: Capability[];

// @public
export interface AllowExpr {
    // (undocumented)
    readonly roles: readonly string[];
    // (undocumented)
    readonly _tag: 'allow';
}

// @public
export interface AndExpr {
    // (undocumented)
    readonly exprs: readonly AuthExpression[];
    // (undocumented)
    readonly _tag: 'and';
}

// @public
export function assertPublicUrl(rawUrl: string): void;

// @public
export const AUTH_ACTIONS: readonly ["read", "create", "update", "write", "delete", "share", "admin"];

// @public
export type AuthAction = (typeof AUTH_ACTIONS)[number];

// @public
export interface AuthCheckInput {
    action: AuthAction;
    node?: {
        schemaId: SchemaIRI;
        createdBy: DID$1;
        properties?: Record<string, unknown>;
    };
    nodeId: string;
    patch?: Record<string, unknown>;
    // Warning: (ae-forgotten-export) The symbol "DID$1" needs to be exported by the entry point index.d.ts
    subject: DID$1;
}

// @public
export interface AuthDecision {
    action: AuthAction;
    allowed: boolean;
    cached: boolean;
    duration: number;
    evaluatedAt: number;
    grants: string[];
    reasons: AuthDenyReason[];
    resource: string;
    roles: string[];
    subject: DID$1;
}

// @public
export type AuthDenyReason = 'DENY_NODE_POLICY' | 'DENY_NO_ROLE_MATCH' | 'DENY_NO_GRANT' | 'DENY_UCAN_INVALID' | 'DENY_UCAN_REVOKED' | 'DENY_UCAN_EXPIRED' | 'DENY_DEPTH_EXCEEDED' | 'DENY_NOT_AUTHENTICATED' | 'DENY_FIELD_RESTRICTED' | 'DENY_GRANT_EXPIRED' | 'DENY_STALE_OFFLINE';

// @public
export interface AuthenticatedExpr {
    // (undocumented)
    readonly _tag: 'authenticated';
}

// @public
export type AuthExpression = AllowExpr | DenyExpr | AndExpr | OrExpr | NotExpr | RoleRefExpr | PublicExpr | AuthenticatedExpr;

// @public
export interface AuthorizationDefinition<TActions extends Record<string, AuthExpression> = Record<string, AuthExpression>, TRoles extends Record<string, RoleResolver> = Record<string, RoleResolver>> {
    actions: TActions;
    fieldRules?: Record<string, {
        allow: AuthExpression;
        deny?: AuthExpression;
    }>;
    nodePolicy?: {
        mode: 'extend';
        allow: ('deny' | 'fieldRules' | 'conditions')[];
    };
    publicProps?: string[];
    roles: TRoles;
}

// @public
export interface AuthTrace extends AuthDecision {
    steps: AuthTraceStep[];
}

// @public
export interface AuthTraceStep {
    duration: number;
    input: Record<string, unknown>;
    output: Record<string, unknown>;
    phase: 'node-deny' | 'role-resolve' | 'schema-eval' | 'grant-check' | 'public-check';
}

// @public
export const BOOTSTRAP_PEERS: readonly ["/dns4/bootstrap1.xnet.io/tcp/4001/p2p/12D3KooWBootstrap1", "/dns4/bootstrap2.xnet.io/tcp/4001/p2p/12D3KooWBootstrap2"];

// @public
export function buildMerkleTree(chunks: ContentChunk[]): ContentTree;

// @public
export type Capability = 'read' | 'create' | 'update' | 'write' | 'delete' | 'share' | 'admin';

// @public
export function capped(policy: RetryPolicy, maxDelayMs: number): RetryPolicy;

// @public
export interface ChainStatus {
    // (undocumented)
    errors: string[];
    // (undocumented)
    forks: Fork[];
    // (undocumented)
    valid: boolean;
}

// @public
export function clamp(value: number, min: number, max: number): number;

// @public
export function clamp01(value: number): number;

// @public
export function compareChangeApplicationOrder(a: {
    lamport: number;
    author: string;
}, b: {
    lamport: number;
    author: string;
}): number;

// @public
export function compareLwwStamps(a: LwwStamp, b: LwwStamp): number;

// @public
export function compareVectorClocks(a: VectorClock, b: VectorClock): -1 | 0 | 1;

// @public
export function computeLwwTiebreakKey(author: string, propertyKey: string, value: unknown): string;

// @public
export interface Condition {
    // (undocumented)
    type: 'time' | 'ip' | 'device';
    // (undocumented)
    value: unknown;
}

// @public
export interface ContentChunk {
    // (undocumented)
    data: Uint8Array;
    // (undocumented)
    hash: string;
    // (undocumented)
    size: number;
}

// @public
export type ContentId = `cid:blake3:${string}`;

// @public
export interface ContentResolver {
    buildTree(chunks: ContentChunk[]): ContentTree;
    get(cid: ContentId): Promise<Uint8Array | null>;
    put(data: Uint8Array): Promise<ContentId>;
    verify(cid: ContentId, data: Uint8Array): boolean;
}

// @public
export interface ContentTree {
    // (undocumented)
    nodes: Map<string, MerkleNode>;
    // (undocumented)
    rootHash: string;
}

// @public
export function createChunk(data: Uint8Array): ContentChunk;

// @public
export function createContentId(hash: string): ContentId;

// @public
export interface CreatorRoleResolver {
    // (undocumented)
    readonly _tag: 'creator';
}

// @public
export interface DataSource {
    // (undocumented)
    estimatedLatency: number;
    // (undocumented)
    id: string;
    // (undocumented)
    type: 'local' | 'peer' | 'cluster';
}

// @public
export function deduplicatedUnion<T extends {
    id: string;
}>(results: T[][]): T[];

// @public
export const DEFAULT_SNAPSHOT_TRIGGERS: SnapshotTriggers;

// @public
export const DEFAULT_STREAMING_OPTIONS: StreamingQueryOptions;

// @public
export interface DenyExpr {
    // (undocumented)
    readonly roles: readonly string[];
    // (undocumented)
    readonly _tag: 'deny';
}

// @public
export function detectFork(updates: SignedUpdate[]): Fork | null;

// @public
export const DHT_CONFIG: {
    readonly protocol: "/xnet/kad/1.0.0";
    readonly replicationFactor: 20;
    readonly refreshInterval: number;
};

// @public
export type DID = `did:key:${string}`;

// @public
export interface DIDResolution {
    // (undocumented)
    did: string;
    // (undocumented)
    lastUpdated: number;
    // (undocumented)
    locations: PeerLocation[];
    // (undocumented)
    publicKey: Uint8Array;
}

// @public
export interface DIDResolver {
    getCached(did: string): DIDResolution | null;
    publish(did: string, locations: PeerLocation[]): Promise<void>;
    resolve(did: string): Promise<DIDResolution | null>;
}

// @public
export interface DocumentLoad {
    // (undocumented)
    snapshot?: Snapshot;
    // (undocumented)
    updatesSinceSnapshot: SignedUpdate[];
}

// @public (undocumented)
export type DocumentPath = `xnet://${DID}/workspace/${string}/doc/${string}`;

// @public
export function estimateQueryCost(query: Query): number;

// @public
export function evaluateCondition(condition: Condition, context: {
    now?: number;
}): boolean;

// @public
export function exponential(baseMs: number, factor?: number): RetryPolicy;

// @public
export function fixed(delayMs: number): RetryPolicy;

// @public
export interface Fork {
    // (undocumented)
    branch1: SignedUpdate[];
    // (undocumented)
    branch2: SignedUpdate[];
    // (undocumented)
    commonAncestor: string;
}

// @public
export function formatBytes(bytes: number): string;

// @public
export function getMostPermissiveCapability(capabilities: Capability[]): Capability | null;

// @public
export function grantActionSatisfies(granted: AuthAction, checked: AuthAction): boolean;

// @public
export interface Group {
    // (undocumented)
    id: string;
    // (undocumented)
    managedBy: string[];
    // (undocumented)
    memberGroups: string[];
    // (undocumented)
    members: string[];
}

// @public
export function hashContent(data: Uint8Array): string;

// @public
export function incrementVectorClock(clock: VectorClock, peerId: string): VectorClock;

// @public
export interface IPCondition extends Condition {
    // (undocumented)
    type: 'ip';
    // (undocumented)
    value: {
        allowList?: string[];
        denyList?: string[];
    };
}

// @public
export function isLocationFresh(location: PeerLocation, maxAge?: number): boolean;

// @public
export function isTagged<Tag extends string>(error: unknown, tag: Tag): error is TaggedError<Tag>;

// @public
export function isValidDID(did: string): boolean;

// @public
export function isValidProgression(prev: VectorClock, next: VectorClock, authorId: string): boolean;

// @public
export function jittered(policy: RetryPolicy, ratio?: number, random?: () => number): RetryPolicy;

// @public
export function limitAttempts(policy: RetryPolicy, maxAttempts: number): RetryPolicy;

// @public
export const LWW_TIEBREAK_KEY_VERSION = 4;

// @public
export interface LwwStamp {
    // (undocumented)
    author: string;
    // (undocumented)
    lamport: number;
    tiebreakKey?: string;
    // (undocumented)
    wallTime: number;
}

// @public
export function lwwUpdateGuardSql(input: {
    table: string;
    lamportColumn: string;
    wallTimeColumn: string;
    authorColumn: string;
    tiebreakKeyColumn?: string;
}): string;

// @public
export function lwwWins(incoming: LwwStamp, existing: LwwStamp): boolean;

// @public
export interface MembershipRoleResolver {
    readonly containerProp: string;
    readonly edgeSchema: string;
    readonly memberProp: string;
    readonly minRole: string;
    readonly parentProp?: string;
    readonly roleOrder: readonly string[];
    readonly roleProp: string;
    // (undocumented)
    readonly _tag: 'membership';
}

// @public
export function mergeStateVectors(base: Uint8Array, _updates: SignedUpdate[]): Uint8Array;

// @public
export function mergeVectorClocks(a: VectorClock, b: VectorClock): VectorClock;

// @public
export interface MerkleNode {
    // (undocumented)
    children?: string[];
    // (undocumented)
    data?: Uint8Array;
    // (undocumented)
    hash: string;
}

// @public
export interface NotExpr {
    // (undocumented)
    readonly expr: AuthExpression;
    // (undocumented)
    readonly _tag: 'not';
}

// @public
export interface OrExpr {
    // (undocumented)
    readonly exprs: readonly AuthExpression[];
    // (undocumented)
    readonly _tag: 'or';
}

// @public
export function parseContentId(cid: ContentId): string;

// @public
export function parseDID(did: string): {
    method: string;
    identifier: string;
} | null;

// @public
export interface PeerLocation {
    // (undocumented)
    lastSeen: number;
    // (undocumented)
    latency?: number;
    // (undocumented)
    multiaddr: string;
}

// @public
export interface PermissionEvaluator {
    getPermissions(did: string, resource: ResourceScope): Promise<Capability[]>;
    hasCapability(did: string, capability: Capability, resource: ResourceScope): Promise<boolean>;
    resolveGroups(did: string): Promise<string[]>;
}

// @public
export interface PermissionGrant {
    // (undocumented)
    conditions?: Condition[];
    // (undocumented)
    principal: string;
    // (undocumented)
    role: string;
    // (undocumented)
    scope: ResourceScope;
}

// @public
export interface PolicyEvaluator {
    can(input: AuthCheckInput): Promise<AuthDecision>;
    explain(input: AuthCheckInput): Promise<AuthTrace>;
    invalidate(nodeId: string): void;
    invalidateSubject(did: DID$1): void;
}

// @public
export interface PropertyRoleResolver {
    // (undocumented)
    readonly propertyName: string;
    // (undocumented)
    readonly _tag: 'property';
}

// @public
export interface PublicExpr {
    // (undocumented)
    readonly _tag: 'public';
}

// @public
export interface Query {
    // (undocumented)
    filters?: Record<string, unknown>;
    // (undocumented)
    limit?: number;
    // (undocumented)
    offset?: number;
    // (undocumented)
    orderBy?: string;
    // (undocumented)
    orderDirection?: 'asc' | 'desc';
    // (undocumented)
    type: string;
}

// @public
export interface QueryPlan {
    // (undocumented)
    aggregation: 'union' | 'join' | 'custom';
    // (undocumented)
    customAggregator?: (results: unknown[][]) => unknown[];
    // (undocumented)
    subqueries: SubQuery[];
}

// @public
export interface QueryRequest {
    // (undocumented)
    auth: string;
    // (undocumented)
    query: Query;
    // (undocumented)
    queryId: string;
}

// @public
export interface QueryResponse {
    // (undocumented)
    cursor?: string;
    // (undocumented)
    error?: string;
    // (undocumented)
    hasMore: boolean;
    // (undocumented)
    queryId: string;
    // (undocumented)
    results: unknown[];
}

// @public
export interface QueryRouter {
    aggregate(plan: QueryPlan, results: unknown[][]): unknown[];
    findSources(query: Query): Promise<DataSource[]>;
    route(query: Query, source: DataSource): Promise<unknown[]>;
}

// @public
export interface RelationRoleResolver {
    // (undocumented)
    readonly relationName: string;
    // (undocumented)
    readonly _tag: 'relation';
    // (undocumented)
    readonly targetRole: string;
}

// @public
export const RESOLUTION_CACHE_CONFIG: {
    readonly maxEntries: 1000;
    readonly ttl: number;
    readonly staleWhileRevalidate: number;
};

// @public
export type ResolutionStrategy = 'local-cache' | 'connected-peers' | 'dht' | 'bootstrap';

// @public
export interface ResourceScope {
    // (undocumented)
    id: string;
    // (undocumented)
    type: 'workspace' | 'document' | 'block';
}

// @public
export interface RetryPolicy {
    delayFor(attempt: number): number | null;
}

// @public
export interface Role {
    // (undocumented)
    capabilities: Capability[];
    // (undocumented)
    id: string;
}

// @public
export function roleHasCapability(role: Role, capability: Capability): boolean;

// @public
export type RoleKey<TAuth extends AuthorizationDefinition> = keyof TAuth['roles'] & string;

// @public
export interface RoleRefExpr {
    // (undocumented)
    readonly role: string;
    // (undocumented)
    readonly _tag: 'roleRef';
}

// @public
export type RoleResolver = CreatorRoleResolver | PropertyRoleResolver | RelationRoleResolver | MembershipRoleResolver;

// @public
export type SchemaAction<S extends {
    authorization: AuthorizationDefinition;
}> = ActionKey<S['authorization']>;

// @public
export type SerializedAuthExpression = {
    _tag: 'allow';
    roles: string[];
} | {
    _tag: 'deny';
    roles: string[];
} | {
    _tag: 'and';
    exprs: SerializedAuthExpression[];
} | {
    _tag: 'or';
    exprs: SerializedAuthExpression[];
} | {
    _tag: 'not';
    expr: SerializedAuthExpression;
} | {
    _tag: 'roleRef';
    role: string;
} | {
    _tag: 'public';
} | {
    _tag: 'authenticated';
};

// @public
export interface SerializedAuthorization {
    // (undocumented)
    actions: Record<string, SerializedAuthExpression>;
    // (undocumented)
    fieldRules?: Record<string, {
        allow: SerializedAuthExpression;
        deny?: SerializedAuthExpression;
    }>;
    // (undocumented)
    nodePolicy?: {
        mode: 'extend';
        allow: string[];
    };
    // (undocumented)
    publicProps?: string[];
    // (undocumented)
    roles: Record<string, SerializedRoleResolver>;
}

// @public
export type SerializedRoleResolver = {
    _tag: 'creator';
} | {
    _tag: 'property';
    propertyName: string;
} | {
    _tag: 'relation';
    relationName: string;
    targetRole: string;
} | {
    _tag: 'membership';
    edgeSchema: string;
    containerProp: string;
    memberProp: string;
    roleProp: string;
    minRole: string;
    roleOrder: string[];
    parentProp?: string;
};

// @public
export function shouldCreateSnapshot(updateCount: number, lastSnapshotTime: number, storageUsed: number, storageTotal: number, triggers?: SnapshotTriggers): boolean;

// @public
export interface SignedUpdate {
    // (undocumented)
    authorDID: string;
    // (undocumented)
    parentHash: string;
    // (undocumented)
    signature: Uint8Array;
    // (undocumented)
    timestamp: number;
    // (undocumented)
    update: Uint8Array;
    // (undocumented)
    updateHash: string;
    // (undocumented)
    vectorClock: VectorClock;
}

// @public
export function singleFlight<K, V>(map: Map<K, Promise<V>>, key: K, fn: () => Promise<V>, options?: SingleFlightOptions): Promise<V>;

// @public
export interface SingleFlightOptions {
    retain?: 'settled' | 'keep';
}

// @public
export interface Snapshot {
    // (undocumented)
    compressedState: Uint8Array;
    // (undocumented)
    contentId: ContentId;
    // (undocumented)
    creatorDID: string;
    // (undocumented)
    documentId: string;
    // (undocumented)
    id: string;
    // (undocumented)
    signature: Uint8Array;
    // (undocumented)
    stateVector: Uint8Array;
    // (undocumented)
    timestamp: number;
}

// @public
export interface SnapshotTriggers {
    // (undocumented)
    storagePressure: number;
    // (undocumented)
    timeInterval: number;
    // (undocumented)
    updateCount: number;
}

// @public
export class SsrfError extends Error {
    constructor(message: string, url: string);
    // (undocumented)
    readonly url: string;
}

// @public
export const STANDARD_ROLES: Record<string, Role>;

// @public
export interface StreamingQueryOptions {
    // (undocumented)
    batchSize: number;
    // (undocumented)
    maxResults: number;
    // (undocumented)
    timeout: number;
}

// @public
export interface SubQuery {
    // (undocumented)
    estimatedCost: number;
    // (undocumented)
    query: Query;
    // (undocumented)
    source: DataSource;
}

// @public
export abstract class TaggedError<Tag extends string = string> extends Error {
    constructor(message: string, options?: ErrorOptions);
    abstract readonly _tag: Tag;
}

// @public
export interface TimeCondition extends Condition {
    // (undocumented)
    type: 'time';
    // (undocumented)
    value: {
        after?: number;
        before?: number;
    };
}

// @public
export function unionAggregate<T>(results: T[][]): T[];

// @public
export interface UpdateVerifier {
    detectFork(updates: SignedUpdate[]): Fork | null;
    isValidProgression(prev: VectorClock, next: VectorClock, authorId: string): boolean;
    verify(update: SignedUpdate, publicKey: Uint8Array): Promise<boolean>;
}

// @public
export function validateExternalUrl(rawUrl: string): {
    valid: boolean;
    error?: string;
};

// @public
export interface VectorClock {
    // (undocumented)
    [peerId: string]: number;
}

// @public
export function verifyContent(cid: ContentId, data: Uint8Array): boolean;

// @public
export function verifyUpdateChain(updates: SignedUpdate[], getPublicKey: (did: string) => Promise<Uint8Array>, verifySignature: (data: Uint8Array, signature: Uint8Array, publicKey: Uint8Array) => boolean): Promise<ChainStatus>;

// Warnings were encountered during analysis:
//
// dist/index.d.ts:831:9 - (ae-forgotten-export) The symbol "SchemaIRI" needs to be exported by the entry point index.d.ts

// (No @packageDocumentation comment for this package)

```
