/**
 * Tagged-error convention (exploration 0303).
 *
 * Re-exported from the package root so consumers can
 * `import { TaggedError } from '@xnetjs/core'`.
 */
export { TaggedError, isTagged } from './tagged'
