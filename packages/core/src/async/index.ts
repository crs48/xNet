/**
 * Async utilities (exploration 0303).
 *
 * Re-exported from the package root so consumers can
 * `import { singleFlight } from '@xnetjs/core'`.
 */
export { singleFlight, type SingleFlightOptions } from './single-flight'
