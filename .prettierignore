apps/web/src/routeTree.gen.ts

# pnpm's lockfile has a canonical serialization (single-line resolutions).
# Prettier would rewrite every entry to multi-line, producing a huge spurious
# diff and drifting from what `pnpm install` writes — leave it to pnpm.
pnpm-lock.yaml

# Generated by scripts/build-plugin-index.mjs (exploration 0201) — format is
# script-canonical (JSON.stringify); Prettier must not rewrite it or the
# `--check` drift guard would fail.
registry/registry.json
site/public/registry.json
site/public/revoked.json
