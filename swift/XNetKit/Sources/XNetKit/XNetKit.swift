/// XNetKit — a native Swift SDK for XNet's local-first graph database.
public enum XNetKit {
    public static let version = "0.1.0"
    /// The umbrella protocol version this SDK speaks (docs/specs/protocol).
    public static let protocolVersion = "xnet/1.0"
}
