/**
 * Schema definitions for the xNet Expo app
 *
 * Re-exports schemas from @xnetjs/data for use in the app.
 */
import { PageSchema } from '@xnetjs/data'

// Re-export PageSchema as Page for convenience
export const Page = PageSchema
