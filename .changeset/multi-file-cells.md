---
'@xnetjs/views': minor
---

File cells hold several attachments. A file field configured with "Allow multiple files" now accepts multi-select uploads and multi-file drops, showing a chip per file with a "+N" counter when a cell holds more than three. The field menu also exposes accepted-type presets (images, images & video, documents), which are enforced on both upload and drop.
