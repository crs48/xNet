---
'@xnetjs/cli': minor
---

New `xnet data export` (full/space/schema/node scope, incremental
`--since-lamport`), `xnet data import` (with `--dry-run` verify report),
and `xnet data snapshot --sqlite` (VACUUM INTO) commands for `.xnetpack`
bundle portability.
