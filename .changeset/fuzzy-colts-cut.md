---
---

Formatting-only pass over `packages/cli` and `packages/data` (prettier), plus a
browser-safety fix to the private `@xnetjs/social` import barrel. No
consumer-visible change to any published package.
