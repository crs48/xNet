---
'@xnetjs/plugins': minor
---

Add the `surfaceDock` contribution point (exploration 0273): plugins can register `SurfaceDockContribution` panels (`tier: 'hero' | 'secondary'`, group, keywords, badge) that the quiet shell's bottom-right dock launcher renders — the devtools hero/secondary grammar lifted to an app-level registry.
